/** @format */

module.exports.QuoteMap = (source, quote) => ({
	source: source,
	quote: quote,
});

module.exports.QuoteMapCharacter = (character, english, portuguese) => ({
	character,
	english,
	portuguese,
});

